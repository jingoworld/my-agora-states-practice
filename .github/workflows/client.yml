name: client
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout source code.
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
        working-directory: ./my-agora-states-client
      - name: Build
        run: npm run build
        working-directory: ./my-agora-states-client
      - name: SHOW AWS CLI VERSION
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULJNCJLNRF }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws --version
      - name: Sync Bucket
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULJNCJLNRF }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws s3 sync \
            --region ap-northeast-2 \
            build s3://fe-120-jayjin \
            --delete
        working-directory: ./my-agora-states-client
